# This file uses the HOCON syntax (https://github.com/lightbend/config/blob/master/HOCON.md)

contractParameterType7Values = [
  # Удостоверение личности: тип ----------------------------------------------------------------------------------------

  {
    id = ${ID_CARD_TYPE_PASSPORT_RF}
    parent = ${CONTRACT_PARAMETER_ID_CARD_TYPE}
    title = "Паспорт гражданина РФ"
  }
  {
    id = ${ID_CARD_TYPE_PASSPORT_ALLIEN}
    parent = ${CONTRACT_PARAMETER_ID_CARD_TYPE}
    title = "Иностранный паспорт"
  }

  # Пол ----------------------------------------------------------------------------------------------------------------

  {
    id = ${SEX_ID_MALE}
    parent = ${CONTRACT_PARAMETER_SEX}
    title = "Муж"
  }
  {
    id = ${SEX_ID_FEMALE}
    parent = ${CONTRACT_PARAMETER_SEX}
    title = "Жен"
  }
]
